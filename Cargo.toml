[workspace]
resolver = "3"
members = ["crates/lantern-web", "crates/lantern-wasi"]
# The pumpkin submodule is its own workspace; path deps reach into it.
exclude = ["pumpkin"]

[profile.release]
opt-level = 3
lto = true
